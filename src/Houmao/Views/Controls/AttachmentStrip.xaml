<UserControl x:Class="Houmao.Views.Controls.AttachmentStrip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="Transparent">
    
    <ItemsControl ItemsSource="{Binding Attachments}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Background="{DynamicResource Surface}"
                        CornerRadius="6"
                        Padding="8,6"
                        Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <!-- 图标 -->
                        <TextBlock Text="{Binding Type, Converter={StaticResource AttachmentTypeToIconConverter}}"
                                   FontSize="14"
                                   Foreground="{DynamicResource Text}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        
                        <!-- 文件名 -->
                        <TextBlock Text="{Binding FileName}" 
                                   Foreground="{DynamicResource Text}"
                                   VerticalAlignment="Center"
                                   MaxWidth="150"
                                   TextTrimming="CharacterEllipsis"/>
                        
                        <!-- 删除按钮 -->
                        <Button Content="×" 
                                Width="16" 
                                Height="16"
                                Style="{DynamicResource WindowButtonStyle}"
                                Command="{Binding DataContext.RemoveAttachmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                ToolTip="Remove"
                                Margin="8,0,0,0"/>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>